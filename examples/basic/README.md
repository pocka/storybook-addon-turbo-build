# TurboBuild Addon Basic Example

This directory contains a very basic Storybook for React example with TurboBuild Addon enabled.

Just modified `.storybook/main.js` after `npx -p @storybook/cli sb init`.
